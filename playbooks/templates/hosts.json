{
    "nova_compute": {
      {% for i in range(9) %}
        "compute{{ (index - 1) * vms_per_host + i + start_index }}": "{{ (index - 1) * vms_per_host + i + start_index }}",
      {% endfor %}
        "compute{{ (index - 1) * vms_per_host + 9 + start_index }}": "{{ (index - 1) * vms_per_host + 9 + start_index }}"
    }
}
